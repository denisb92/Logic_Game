<set xmlns:android="http://schemas.android.com/apk/res/android"
    android:fillAfter="true">

    <rotate xmlns:android="http://schemas.android.com/apk/res/android"
        android:fromDegrees="0"
        android:toDegrees="1080"
        android:pivotX="50%"
        android:pivotY="50%"
        android:duration="16000" />
    <!--Keeps rotating the panda image-->

    <translate
        android:toYDelta="530"
        android:duration = "4000"
        android:fillAfter="true"/>
    <!--Moves the panda image down for a duration of 4000-->

    <translate
        android:startOffset = "4000"
        android:toXDelta="780"
        android:duration = "4000"
        android:fillAfter="true" />
    <!--Moves the panda image to the right for a duration of 4000-->

    <translate

        android:toYDelta="-540"
        android:startOffset = "8000"
        android:duration = "4000"
        android:fillAfter="true"/>
    <!--Moves the panda image up for a duration of 4000-->
    <translate
        android:startOffset = "12000"
        android:toXDelta="640"
        android:duration = "4000"
        android:fillAfter="true" />
    <!--Moves the panda image to the right for a duration of 4000-->
    </set>